import React from "react";
import { Box, Typography } from "@mui/material";

const page = () => {
  return (
    <Box>
      <Typography>About Us Page</Typography>
    </Box>
  );
};

export default page;
